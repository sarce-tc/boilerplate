# /arq-domain — Domain entities

Leer: `Domain/Entities/Order.cs` ← AGENT ENTRY POINT (todas las firmas y reglas)

**Regla no obvia:** invariantes solo en la entidad — nunca duplicar como guards en el handler.
